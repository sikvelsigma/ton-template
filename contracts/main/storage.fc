;; storage functions
global int storage::id;

() storage::load() impure inline {
    var ds = get_data().begin_parse();
    storage::id = ds~load_uint64();
    ds.end_parse();
}

() storage::save() impure inline {
    set_data(begin_cell()
        .store_uint64(storage::id)
    .end_cell());
}